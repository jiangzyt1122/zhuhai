name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      VITE_YOUDAO_APP_KEY: ${{ secrets.YOUDAO_APP_KEY }}
      VITE_YOUDAO_APP_SECRET: ${{ secrets.YOUDAO_APP_SECRET }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Youdao JSONP secrets
        run: |
          if [ -z "$VITE_YOUDAO_APP_KEY" ]; then
            echo "Missing GitHub secret: YOUDAO_APP_KEY"
            exit 1
          fi

          if [ -z "$VITE_YOUDAO_APP_SECRET" ]; then
            echo "Missing GitHub secret: YOUDAO_APP_SECRET"
            exit 1
          fi

      - name: Build site
        run: npm run build:pages

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: offline-dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4.0.5
